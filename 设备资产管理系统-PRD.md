# 公司设备资产管理系统 · 产品需求文档(PRD)

版本 v0.9 · 2026-08-27

> v0.9 变更:标签码由二维码改为 Code 128-C 一维码(打印精度所迫),
> 设备增加 6 位纯数字条码号。
>
> v0.8 变更:接入数据库迁移;新增报修记录、成套借用、Excel 批量导入、
> 操作日志页面、事件推送;移除序列号扫码(使用场景少)。
>
> v0.7 变更:手机端拆成独立前端入口(Vant),新增移动版台账。
>
> v0.6 变更:PWA 图标与 service worker 补全、「添加到主屏幕」引导页、后台窄屏适配。
>
> v0.5 变更:设备复制、序列号扫码录入、移动端账号菜单。
>
> v0.4 变更:新增「盘库」(滚动盘点),从阶段二提前实现。
>
> v0.3 变更:新增「采购公司」实体与管理页面,设备可关联采购来源。
>
> v0.2 变更:明确二维码「防外部读取」的设计意图与安全边界;登录方式确定为用户名密码(不接 SSO / 扫码登录);客户端第一阶段只做网页应用(桌面后台 + 移动网页);技术选型收敛为单一方案。

---

## 1. 背景与目标

内部有大量设备(电脑、拍摄/直播器材、外设等)需要统一管理。目前没有系统化台账,设备归属、状态、借用情况靠人工记录,容易丢失、扯皮。

调研过开源方案 Snipe-IT,功能够用,但界面与配套 App 以英文为主(中文需自行本地化且不完整),自托管部署链路(反代/证书/CDN)对纯内部使用偏重。故决定自建一套**轻量、全中文、贴合内部流程**的设备资产管理系统。

**核心目标**

- 设备一处录入,自动生成资产编号 + 二维码
- 二维码打印成 **12mm 标签**贴到设备上
- 手机**在本系统内扫码**即可查看设备详情、办理借用/归还
- 后台一览所有设备的状态与流转历史
- **全中文**,自托管,可部署在现有 Docker 环境

**明确不做(至少 MVP 不做)**:复杂折旧财务、多租户、LDAP/SSO、企业微信/飞书扫码登录、采购审批流、原生 App。

---

## 2. 用户与角色

| 角色 | 说明 | 主要能力 |
|---|---|---|
| 管理员 | IT / 行政负责人 | 设备增删改、生成/打印标签、管理用户、查看全部台账与日志 |
| 普通用户 | 领用设备的员工 | 扫码查看设备、借用/归还、查看自己名下设备 |

MVP 只做「管理员 + 普通用户」两级,后续再细分。

---

## 3. 功能需求

### 3.1 设备录入与管理
- 字段:资产编号(自动)、名称、分类、品牌/型号、序列号(SN)、状态、存放位置、责任人、**采购公司**、采购日期、备注、照片(可选)
- 分类可自定义(如 电脑 / 相机 / 镜头 / 直播设备 / 外设)
- 支持编辑、停用 / 报废、删除(软删除,保留历史)
- **复制**:一次采购多台同型号设备时,带着原设备的字段开新增表单,资产编号
  保存时自动递增。**序列号不复制** —— SN 是每台实物独有的,复制过去会造出两台
  SN 相同的设备,报修和保修时分不清是哪台
- **序列号扫码录入**:厂商贴的 SN 条码码制五花八门,支持一维码(Code128 /
  Code39 / EAN / UPC 等)、二维码、**DataMatrix**。与资产标签的扫码器不同 ——
  那里刻意只认 QR 以减少误读,这里必须放开
- Excel 批量导入 / 导出(阶段二)

**采购公司**

- 独立实体,可增删改;字段:公司名称(唯一)、联系人、联系电话、备注
- 设备可关联一家采购公司,**可留空**(历史遗留设备往往没有采购记录)
- 管理页面可查看某公司名下的全部设备
- **名下还有设备时不允许删除公司**(含已软删除的设备)。设备上的采购来源是历史
  事实,不能因为删掉公司记录就凭空消失;要删先把这些设备的采购公司改掉或清空
- 用途:设备报修/保修时能立刻查到从哪家买的、找谁

**责任人与借用人的区别(重要)**

系统内存在两种流转模式,字段语义必须分开:

- `owner_user_id`(**长期责任人**):如员工的笔记本,一发数年。由管理员手工指定,借还流程**不修改**该字段。
- **当前借用人**:如相机/镜头/直播设备,按次借还。**不单独存字段**,由「该设备最新一条未归还的借还记录」派生得出。

### 3.2 资产编号与二维码 ★(12mm 标签是硬需求,重点)

**编号规则**
- `分类前缀 + 4 位流水`,如 `PC-0001`、`CAM-0012`;前缀与位数可配置
- **资产编号一经生成永不变更**。标签已贴在实物上,改分类不改编号;分类前缀仅在生成时使用一次
- 支持导入存量设备时手工指定编号
- 流水号自增依赖数据库唯一约束 + 冲突重试,不在应用层做「读-加-写」

> **v0.9 起改用 Code 128-C 一维码**。原因是打印精度:12mm 标签上 QR 每模块
> 只有 0.31mm = 203dpi 打印机的 2.5 个点,不是整数倍,模块边缘参差,实际打样
> 扫不出来。一维码只在一个方向要精度,条宽可取 0.375mm(正好 3 个点)。
> 设备因此增加 `barcode` 字段(6 位纯数字,C 子集只能编数字),标签上仍印
> `asset_tag` 供人工识别,两者都能查到设备。以下关于「码里不放 URL」的约定
> 对条码同样适用。

**二维码内容 = 纯资产编号字符串**(如 `PC-0001`),**不放 URL**。

设计意图:**让二维码在本系统之外被扫到时读不出任何信息**。用任意扫码器扫这张标签,只会得到一串 `PC-0001`,无法跳转、无法查询、看不出是什么设备、归谁、在哪。要看到设备信息,必须在本系统的移动网页中登录后扫码。

**安全边界(必须遵守)**

- 二维码是**标识符,不是凭证**。严禁设计任何「扫到码即可免登录查看/借还」的捷径——标签贴在设备外壳上,拍张照就等于拿到凭证
- 查详情、借出、归还一律走登录后的接口鉴权
- `GET /api/assets/by-tag/{tag}` 必须鉴权,并按用户限流(60 次/分钟),异常频次记入操作日志,防止按编号规律枚举全量台账
- 编号查不到时统一返回 404,不返回「该编号未注册」之类可确认编号空间的提示

**二维码生成**
- 每台设备生成对应二维码图片(PNG / SVG),可单个下载或批量导出
- **码制:标准 QR(非 Micro QR)**。Micro QR 虽更小,但 ZXing 与浏览器
  `BarcodeDetector` 均不支持,打出来本系统自己的扫码页读不出
- **纠错等级 H(30%)**。version 1 在 H 级下仍可容纳 10 个字母数字字符,
  码的尺寸一格未变,抗磨损能力却从 15% 提到 30%。设备标签会蹭脏、磨损、
  被手指盖住一角,这个余量是免费拿到的
- **编号长度上限 10 个字符**(前缀 ≤5 + 连字符 + 4 位流水)。超出即跳到
  version 2(25×25),模块变小,12mm 标签明显更难扫。故分类前缀限长 5
- **静默区 4 个模块**(QR 规范要求),不可为省空间压缩

### 3.3 标签打印
- 标签版式:二维码在左 + 编号文字在右,码磨损后仍能人工识别并手动输入
- **MVP**:导出「资产编号列表」或二维码图片包,用标签机自带 App(精臣 NIIMBOT 云打印支持 Excel/CSV 批量生成二维码)排版打印 12mm 标签,省去自己做打印排版
- **后续**:系统内置 12mm 标签模板,勾选设备 → 一键生成可打印页

**开工前置动作(在写码前完成)**:用真标签机打 3 张样,分别用一台低端安卓机和一台旧 iPhone 实测扫描。12mm 标签实际可打印宽度仅 9~10mm,QR version 1(21×21)每模块约 0.43mm,已接近打印机极限。**以实测结果反过来确定编号长度与纠错等级**,不要先写代码。

### 3.4 扫码查看(移动网页)★

**客户端形态**:移动网页(PWA),手机浏览器打开,调用摄像头扫码。第一阶段不做原生 App、不做企业微信/飞书内嵌。

**独立前端入口**:手机端与桌面后台是两套独立打包的前端(`/m/*` 用 Vant,
`/admin/*` 用 Element Plus),共享后端接口与 UI 无关的工具层。这样手机用户不必
下载桌面组件库,将来手机端套 App 壳或重写成小程序时也只动这一半。
底部标签栏:扫码 / 台账 / 我的。

> 注意:小程序是**彻底重写**而非移植 —— 它有自己的模板语法和运行时,跑不了 Vue
> 组件。真正需要为小程序提前准备的是**后端支持 Token 认证**(小程序不能可靠地用
> Cookie),这一项尚未做。

由于二维码内容不是 URL,员工**无法用微信或系统相机直接进入**,必须先打开本系统。因此降低「打开应用」的摩擦是本模块的核心设计目标:

- 提供 **PWA manifest + 图标 + service worker**,并提供一页「添加到主屏幕」引导
  (`/m/install`,分 iOS / 安卓两套步骤,支持 Chrome 的一键安装)。
  iOS 不读 manifest 的 icons,必须单独提供 `apple-touch-icon`,否则主屏图标是页面截图
- 后台页面窄屏适配:管理员会拿手机开后台,表格换卡片、筛选收进抽屉
- **移动端首页即扫码取景框**,打开页面摄像头即启动,不要先展示菜单
- 取景框正下方常驻**手动输入编号**入口(标签磨损、对焦失败、摄像头权限被拒时的兜底)
- 登录态保持 **30 天**,避免每次打开都要重新登录
- 页头账号菜单:修改密码、退出登录(管理员另有「后台管理」入口)。普通用户
  看不到后台导航栏,没有这个菜单就无法退出账号
- 支持**连续扫描模式**(扫完不退出、继续扫下一台),用于盘点

**扫码实现**
- 优先使用浏览器原生 `BarcodeDetector`(Android Chrome 支持,性能最佳)
- Safari 不支持,回退 `@zxing/browser`(wasm)
- 摄像头调用要求 HTTPS(安全上下文),部署时必须保证

**扫码后**:拉取设备详情——名称、状态、当前责任人 / 当前借用人、借用历史;页面直接提供「借出 / 归还」按钮。

**风险与排期要求**:摄像头调用是本项目唯一可能做不成的技术点。**第一个迭代先只做扫码页面**(开摄像头 → 扫出字符串 → 显示在屏幕上),在真机(至少一台 iPhone + 一台 Android)上跑通后,再开发其余功能。iOS 需分别验证 Safari 普通标签页与「添加到主屏幕」后的 standalone 模式。

### 3.5 借用与归还(领用流转)★
- **借出**:扫码/选择设备 → 领用人默认为当前登录用户 → 记录 领用时间、预计归还时间 → 状态置「借出」
- **归还**:记录 实际归还时间 → 状态回「在库」。**任何登录用户均可代为归还**(器材常由同事顺手带回),但须记录 `operator_id`
- 每台设备保留完整流转历史(谁、何时、借/还、经办人)
- 逾期未还高亮提示;支持按人查看「某人名下所有设备」

**状态一致性**
- `status` 只表达设备自身状态:在库 / 维修 / 报废
- 「借出中」为**派生状态**,由「存在 `checked_in_at` 为空的借还记录」判定,不冗余存储,避免与借还记录打架
- 设备编辑表单**不允许**手动把状态改为「借出」

**并发控制**
- 借出必须使用条件更新(`WHERE 该设备不存在未归还记录`)而非「先查后写」,防止两人同时扫同一台设备各自借出成功

**成套借用(阶段三,但数据模型预留)**
相机 + 镜头 + 电池 + 三脚架常需一起借出。MVP 不实现,但借还记录表预留 `kit_id`,避免事后改表。

### 3.6 台账 / 看板
- 设备列表:分页、搜索(编号/名称/SN/责任人)、按分类/采购公司/状态/位置/超期未盘筛选
- **移动版台账**:搜索 + 卡片列表 + 上拉加载 + 筛选弹层,手机上不必跳回桌面后台
- 导出当前列表(Excel / CSV)
- 概览看板:设备总数,在库 / 借出 / 维修 / 报废 数量分布 —— **移出 MVP,列入阶段二**

### 3.7 用户与权限

- 账号登录(用户名 + 密码),**不做 SSO、不做企业微信/飞书扫码登录**
- 角色:管理员 / 普通用户
- 移动扫码页与桌面后台**共用同一套登录**
- **不开放自助注册**,账号一律由管理员创建
- 管理员创建账号时发放初始密码,用户**首次登录强制改密**
- **忘记密码 = 找管理员重置**,MVP 不做邮件找回流程

**认证实现**

- 密码哈希使用 **argon2id**(或 bcrypt cost ≥ 12)
- 登录限流:同一账号连续失败 5 次锁定 15 分钟;同一 IP 独立限流。此项优先级高于密码复杂度要求
- 前端静态文件由后端同域托管 → 无 CORS
- JWT 存放于 **httpOnly + SameSite=Lax Cookie**,而非 localStorage。同域 + SameSite=Lax 已可防御跨站 CSRF,同时 XSS 无法窃取 token
- Token 有效期 30 天

### 3.9 盘库(滚动盘点)★

定期核对设备的状态、位置、使用情况,并留下「谁在什么时候盘的」。

**为什么不做「盘点任务」**

盘库的价值在于发现**少了什么**。而「扫一台记一条」只能记录扫到的东西 ——
丢失的那台没人扫,不会产生任何记录,它的缺席是隐形的。所以必须有办法回答
「还有哪些没盘到」。

两种做法:建盘点任务实体、跟踪已盘未盘、结束出盘盈盘亏报告(轮次盘点);
或者给每台设备算出「最后盘库时间」,按「超过 N 天未盘库」筛选(滚动盘点)。

**采用滚动盘点**:公司规模数百台、IT 行政仅一两人,轮次盘点排期难、容易半途
而废;滚动盘点是持续的,超期列表随时就是待办清单,清空即等于盘完一轮。
以后要加轮次盘点也不冲突。

**记录内容**

- 盘库人、盘库时间
- 实际位置、实际状态、使用情况备注
- **盘库当时台账里的位置与状态(快照)**。必须存下来 —— 台账后来又被改过的话,
  光比对当前值就对不上了
- 盘库当时的借用人(在同事桌上盘到设备是正常情况,不算异常)

**权限与差异处理**

- **任何登录用户都能提交盘库记录**。记录的是观察值,永远提交成功 —— 现场盘库
  的人不该因为权限或状态冲突卡在半路
- **管理员**盘出的差异当场写回台账
- **普通用户**盘出的差异挂起,进「待处理差异」列表,管理员可**采纳**(写回台账)
  或**忽略**(维持台账,仅留档)
- 借出中的设备不允许改状态(见 3.5),所以即便是管理员,状态差异也只挂起,
  等归还后再处理;位置仍可当场修正

**入口**

- 扫码后的设备详情页:「盘库」按钮(单台复核)
- 移动扫码页「连续扫码 + 盘库模式」:**每扫到一台就直接记一条「确认无误」**,
  对不上的才点进去改。盘点的真实动作是拿着手机连扫几百台,每台都点进详情
  根本坚持不下来
- 后台台账:「盘库」按钮、「最后盘库」列、「超过 N 天未盘库」筛选
- 后台盘库页:概览(在册 / 已盘 / 超期 / 待处理差异)+ 盘库流水 + 差异处理

### 3.8 操作日志(阶段二)
- 关键操作留痕:新增 / 编辑 / 删除设备、借出、归还、导入、by-tag 查询异常频次

---

## 4. 非功能需求
- **语言**:全中文(界面、提示、导出)
- **部署**:Docker Compose,跑在现有服务器
- **访问**:支持外网访问(公司外也能扫),经 Caddy 反代 + 自动 HTTPS,登录鉴权保护
- **规模**:数百 ~ 数千台设备,单机足够
- **安全**:见 3.2 安全边界与 3.7 认证实现;二维码只含编号不含敏感信息
- **备份**:SQLite 数据库文件 + 设备照片目录定期备份(可接现有备份方案)

---

## 5. 数据模型

**assets(设备)**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | PK | |
| asset_tag | string, unique | 资产编号,如 PC-0001,生成后不可变更 |
| barcode | string(6), unique | 条码号,6 位纯数字,仅用于 Code 128-C 标签 |
| name | string | 设备名称 |
| category_id | FK | 分类 |
| brand / model | string | 品牌 / 型号 |
| serial_no | string | SN |
| status | enum | 在库 / 维修 / 报废(**不含「借出」,借出为派生状态**) |
| location | string | 存放位置 |
| owner_user_id | FK, null | 长期责任人(借还流程不修改) |
| company_id | FK, null | 采购公司,可空 |
| purchased_at | date, null | 采购日期 |
| note | text | 备注 |
| photo_url | string, null | 照片 |
| created_at / updated_at / deleted_at | datetime | 软删除 |

**categories(分类)**:`id, name, tag_prefix(编号前缀, unique), seq(当前流水号)`

**companies(采购公司)**:`id, name(unique), contact, phone, note, created_at`

**users(用户)**:`id, username(unique), password_hash, real_name, role, department, must_change_password, failed_attempts, locked_until`

**checkout_records(借还记录)**:`id, asset_id, user_id(领用人), checked_out_at, due_at, checked_in_at, operator_id(经办人), kit_id(预留), note`
> 唯一部分索引:同一 asset_id 至多一条 `checked_in_at IS NULL` 的记录

**inventory_checks(盘库记录)**:`id, asset_id, checked_by_id, checked_at,
observed_location, observed_status, location_at_check, status_at_check(台账快照),
borrower_id(可空), note, applied, resolved_at, resolved_by_id`
> 「最后盘库时间」与「是否有差异」均为派生,不在 assets 上冗余字段

**activity_logs(操作日志)**:`id, actor_id, action, target_type, target_id, detail, created_at`

---

## 6. 关键接口(草案)

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/login` | 登录,下发 httpOnly Cookie |
| POST | `/api/auth/logout` | 登出 |
| GET | `/api/auth/me` | 当前用户 |
| POST | `/api/auth/change-password` | 改密(首次登录强制) |
| GET | `/api/assets` | 列表(分页 / 搜索 / 按分类、采购公司、状态、超期未盘筛选) |
| POST | `/api/assets` | 新增(返回自动生成的 asset_tag) |
| GET | `/api/assets/{id}` | 详情 |
| PUT | `/api/assets/{id}` | 编辑 |
| DELETE | `/api/assets/{id}` | 软删除 |
| GET | `/api/assets/by-tag/{tag}` | ★ 扫码查询(鉴权 + 限流) |
| GET | `/api/assets/{id}/label` | ★ 取 Code 128-C 条码(矢量 SVG,可调条宽) |
| GET | `/api/assets/{id}/qrcode` | 取二维码图片(旧标签兼容) |
| POST | `/api/assets/qrcodes/export` | 批量导出二维码 / 编号列表 |
| POST | `/api/assets/{id}/checkout` | 借出(body: user_id, due_at) |
| POST | `/api/assets/{id}/checkin` | 归还 |
| GET | `/api/assets/{id}/history` | 流转历史 |
| GET | `/api/checkouts?overdue=1` | 逾期列表 |
| POST | `/api/assets/{id}/check` | ★ 盘库(位置/状态留空 = 确认无误) |
| GET | `/api/assets/{id}/checks` | 该设备盘库历史 |
| GET | `/api/inventory/checks?pending=1` | 盘库流水 / 待处理差异 |
| GET | `/api/inventory/summary` | 盘库概览(已盘 / 超期 / 待处理) |
| POST | `/api/inventory/checks/{id}/resolve` | 处理差异(apply / dismiss) |
| GET | `/api/me/assets` | 我名下的设备 |
| GET | `/api/categories` `POST` `PUT` `DELETE` | 分类管理 |
| GET | `/api/companies` `POST` `PUT` `DELETE` | 采购公司管理 |
| GET | `/api/users` `POST` `PUT` | 用户管理 |
| GET | `/api/dashboard/summary` | 看板统计(阶段二) |

预留:**webhook**(借出 / 逾期时推给 n8n → 企业微信 / 飞书通知)。

---

## 7. 技术选型(已定)

- **后端**:FastAPI + SQLAlchemy + Alembic(迁移)
- **数据库**:**SQLite(WAL 模式)**。数千台设备、十余并发用户绰绰有余,省掉一个容器,备份即拷贝单文件
- **二维码**:**segno**(纯 Python 无依赖,PNG / SVG 均可)
- **密码哈希**:argon2-cffi
- **前端**:Vue3 + Vite + Element Plus,构建为静态文件由 FastAPI 直接托管(同域、无 CORS、单容器)
  - 同一项目两套路由:`/admin` 桌面台账、`/m` 移动扫码借还
- **扫码**:原生 `BarcodeDetector` 优先,回退 `@zxing/browser`
- **鉴权**:JWT over httpOnly Cookie
- **部署**:Docker Compose = Caddy(自动 HTTPS 反代)+ 应用容器,`docker compose up` 即可

---

## 8. 里程碑

**迭代 0 —— 技术验证(先做)**
标签打样实测扫描(3.3);移动扫码页真机跑通(3.4)。两项均通过后再进入 MVP 开发。

**MVP(阶段一)—— 先能用**
设备录入 → 编号 + 二维码生成 → 台账列表/搜索 → 借出/归还 → 移动扫码页 → 登录鉴权 → Docker 部署,全中文。
> 已从 MVP 移出:概览看板、设备照片、存放位置。

**阶段二 —— 好用**
~~Excel 导入导出~~、~~逾期提醒~~、~~操作日志~~ 已完成;
剩余:概览看板、设备照片、内置 12mm 标签打印。
> 盘点已提前到 3.9 实现(滚动盘点);存放位置随盘库一并提前。
> 阶段三的报修工单、成套借用、n8n 通知也已提前实现。

**阶段三 —— 扩展**
成套借用、折旧、报修工单、n8n / 企业微信通知、多部门 / 多位置、数据看板增强。

---

## 9. 未来可扩展
- 轮次盘点:建盘点任务、跟踪已盘未盘、结束出盘盈 / 盘亏报告(滚动盘点已覆盖日常需求,此项按需再加)
- 与 n8n 联动:逾期 / 借出自动通知到企业微信 / 飞书
- 设备维保记录、附件(发票 / 保修卡)
- 企业微信自建应用内嵌(免密登录 + 原生扫码器),作为网页版之外的第二入口
